

class Mutation():
    def __init__(self):
        self.mutation_iter = []

    def init_mutation_iter(self, mutation_type):
        length = len(Selection.sampler)
        assert not length <= 0

        for i in range(length):
            pass

    def mutate(self):
        pass